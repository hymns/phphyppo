	/**
	 * view
	 *
	 * view / show data from table
	 *
	 * @access public
	 * @return array
	 */
	public function view(${tablename}_id)
	{
		// get data detail
		return $this->_data(${tablename}_id);
	}

